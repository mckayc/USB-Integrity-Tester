<UserControl x:Class="UsbIntegrityTester.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="900">
    <DockPanel Margin="24">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,4">
            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8"
                    Width="40" Height="40" Margin="0,0,12,0">
                <ui:SymbolIcon Symbol="DocumentBulletListClock20" FontSize="20"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="History" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <ui:Button Content="Refresh" Command="{Binding RefreshHistoryCommand}" Padding="12,6" VerticalAlignment="Center">
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="ArrowSync24"/>
                </ui:Button.Icon>
            </ui:Button>
        </StackPanel>

        <TextBlock DockPanel.Dock="Top" Margin="52,0,0,16" TextWrapping="Wrap" FontSize="12"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Text="Every test run is logged here by drive serial number, so you can track the same physical drive over time or across videos."/>

        <DataGrid ItemsSource="{Binding HistoryEntries}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  Background="Transparent" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding TimestampUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140"/>
                <DataGridTextColumn Header="Drive" Binding="{Binding DriveLabel}" Width="180"/>
                <DataGridTextColumn Header="Serial" Binding="{Binding SerialNumber}" Width="140"/>
                <DataGridTextColumn Header="Verdict" Binding="{Binding Verdict}" Width="130"/>
                <DataGridTextColumn Header="Claimed (GB)" Width="100">
                    <DataGridTextColumn.Binding>
                        <Binding Path="ClaimedCapacityBytes" StringFormat="{}{0:N1}" Converter="{StaticResource BytesToGbConverter}"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Verified (GB)" Width="100">
                    <DataGridTextColumn.Binding>
                        <Binding Path="VerifiedGoodBytes" StringFormat="{}{0:N1}" Converter="{StaticResource BytesToGbConverter}"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Write MB/s" Binding="{Binding AverageWriteMegabytesPerSecond, StringFormat='{}{0:N1}'}" Width="90"/>
                <DataGridTextColumn Header="Read MB/s" Binding="{Binding AverageReadMegabytesPerSecond, StringFormat='{}{0:N1}'}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
