<UserControl x:Class="UsbIntegrityTester.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="760">
    <DockPanel Margin="24">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,4">
            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8"
                    Width="40" Height="40" Margin="0,0,12,0">
                <ui:SymbolIcon Symbol="History24" FontSize="20"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="Logs" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
        </StackPanel>
        <TextBlock DockPanel.Dock="Top" Text="Every notable action gets a timestamped entry here — copy it when something fails."
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" Margin="52,0,0,16" TextWrapping="Wrap"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <ui:Button Content="{Binding CopyLogsButtonText}" Command="{Binding CopyLogsCommand}"
                       Width="150" Margin="0,0,8,0" Padding="12,6">
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="Copy24"/>
                </ui:Button.Icon>
            </ui:Button>
            <ui:Button Content="Clear" Command="{Binding ClearLogsCommand}" Padding="12,6">
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="Delete24"/>
                </ui:Button.Icon>
            </ui:Button>
        </StackPanel>

        <ListView ItemsSource="{Binding Logs}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Background="Transparent" BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="8,5"/>
                    <Setter Property="Margin" Value="0,0,0,2"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ui:SymbolIcon Grid.Column="0" FontSize="14" VerticalAlignment="Top" Margin="0,1,8,0"
                                       Foreground="{Binding Level, Converter={StaticResource LogLevelToBrushConverter}}"
                                       Symbol="{Binding Level, Converter={StaticResource LogLevelToIconConverter}}"/>

                        <TextBlock Grid.Column="1" Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}"
                                   FontFamily="Cascadia Mono, Consolas" FontSize="11"
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" VerticalAlignment="Top"/>

                        <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap"
                                   FontFamily="Cascadia Mono, Consolas" FontSize="12"
                                   Foreground="{Binding Level, Converter={StaticResource LogLevelToBrushConverter}}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</UserControl>
